{#
/**
 * @file
 * Default theme implementation to list dependent content types available for adding content.
 *
 * This list is displayed on the Add dependent content admin page.
 *
 * Available variables:
 * - types: A list of dependent content types, each with the following properties:
 *   - label: Label of this type of dependent content.
 *   - link: Link to create a piece of content of this type.
 *   - url: Path to the add dependent content of this type page.
 *   - description: Description of this type of dependent content.
 *
 * @see template_preprocess_dependent_content_add_list()
 *
 * @ingroup themeable
 */
#}

{% if types is not empty %}
  <ul class="admin-list">
    {% for type in types %}
      <li class="clearfix">
        <a href="{{ type.url }}">
          <span class="label">{{ type.label }}</span>
          <div class="description">{{ type.description }}</div>
        </a>
      </li>
    {% endfor %}
  </ul>
{% else %}
  <p>
    {% set create_content = path('entity.dependent_content_type.add_form') %}
    {% trans %}
      You have not created any content types yet. Go to the <a href="{{ create_content }}">content type creation page</a> to add a new content type.
    {% endtrans %}
  </p>
{% endif %}
